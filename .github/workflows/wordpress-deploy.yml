name: Deploy Complete99 WordPress

on:
  workflow_dispatch:
    inputs:
      rollback_exercise:
        description: Exercise rollback and redeploy (requires an existing healthy release)
        required: true
        default: true
        type: boolean
      bootstrap_code_snippets:
        description: Install the approved WordPress.org Code Snippets plugin if absent
        required: true
        default: true
        type: boolean
      orphaned_rollback_proof:
        description: Reviewed repository-relative proof for one orphaned rollback lock
        required: false
        default: ''
        type: string
      orphaned_rollback_observe_only:
        description: Capture a reviewed, non-secret orphaned rollback database observation without deployment
        required: true
        default: false
        type: boolean
      interrupted_forward_proof:
        description: Reviewed repository-relative proof for one interrupted forward deployment
        required: false
        default: ''
        type: string
      interrupted_forward_observe_only:
        description: Capture a reviewed, non-secret interrupted forward observation without deployment
        required: true
        default: false
        type: boolean
      recovery_only:
        description: Recover one reviewed interrupted forward deployment without a new platform deployment
        required: true
        default: false
        type: boolean
permissions:
  actions: read
  contents: read

concurrency:
  group: complete99-wordpress-production
  cancel-in-progress: false

jobs:
  require-main:
    runs-on: ubuntu-latest
    steps:
      - name: Refuse a production deployment from any non-main ref
        shell: bash
        run: |
          if [[ "$GITHUB_REF" != "refs/heads/main" ]]; then
            echo "Production deploys are restricted to refs/heads/main." >&2
            exit 1
          fi

  require-production-ready:
    needs: require-main
    runs-on: ubuntu-latest
    steps:
      - name: Require the explicit production readiness switch
        env:
          WP_PRODUCTION_READY: ${{ vars.WP_PRODUCTION_READY }}
        shell: bash
        run: |
          if [[ "$WP_PRODUCTION_READY" != "true" ]]; then
            echo "WP_PRODUCTION_READY must be exactly true before production deployment." >&2
            exit 1
          fi

  require-green-ci:
    needs:
      - require-main
      - require-production-ready
    runs-on: ubuntu-latest
    outputs:
      ci_run_id: ${{ steps.exact_ci.outputs.run_id }}
    steps:
      - name: Require successful WordPress CI for this exact commit
        id: exact_ci
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail
          response="$(gh api \
            --method GET \
            "/repos/${GITHUB_REPOSITORY}/actions/workflows/wordpress-ci.yml/runs?head_sha=${GITHUB_SHA}&branch=main&status=success&per_page=10")"
          RUNS_JSON="$response" python - <<'PY'
          import json
          import os
          runs = json.loads(os.environ["RUNS_JSON"]).get("workflow_runs", [])
          trusted = [
              run
              for run in runs
              if run.get("head_sha") == os.environ["GITHUB_SHA"]
              and run.get("head_branch") == "main"
              and run.get("event") == "push"
              and run.get("conclusion") == "success"
          ]
          if not trusted:
              raise SystemExit("No successful WordPress CI run exists for this exact commit.")
          run_id = trusted[0].get("id")
          if not isinstance(run_id, int) or run_id <= 0:
              raise SystemExit("The exact-commit WordPress CI run ID is invalid.")
          with open(os.environ["GITHUB_OUTPUT"], "a", encoding="utf-8") as output:
              output.write(f"run_id={run_id}\n")
          PY

  deploy:
    needs:
      - require-main
      - require-production-ready
      - require-green-ci
    runs-on: [self-hosted, Windows, X64, complete99-deploy]
    environment: production
    steps:
      - name: Check out approved production source
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          fetch-depth: 1

      - name: Download the immutable exact-commit CI artifact
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093
        with:
          name: complete99-wordpress-package
          path: ${{ runner.temp }}/complete99-verified-release-${{ github.run_id }}-${{ github.run_attempt }}
          github-token: ${{ github.token }}
          repository: ${{ github.repository }}
          run-id: ${{ needs.require-green-ci.outputs.ci_run_id }}

      - name: Validate the downloaded immutable release
        shell: powershell
        run: |
          $releaseDir = Join-Path $env:RUNNER_TEMP "complete99-verified-release-$env:GITHUB_RUN_ID-$env:GITHUB_RUN_ATTEMPT"
          & python --version
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          & python -c "import sys; raise SystemExit('Python 3.11 or newer is required: ' + sys.version if sys.version_info < (3, 11) else 0)"
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          & python scripts/validate-package.py --dist $releaseDir
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          $bundleFiles = @(Get-ChildItem -LiteralPath $releaseDir -File)
          if ($bundleFiles.Count -ne 4) {
            Write-Error "The immutable release bundle must contain exactly four verified files."
            exit 1
          }
          $approvedCommit = (& git rev-parse HEAD).Trim()
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          if ($approvedCommit -ne $env:GITHUB_SHA) {
            Write-Error "The checked-out production source does not match GITHUB_SHA."
            exit 1
          }

      - name: Validate mutually exclusive recovery inputs before any live request
        env:
          COMPLETE99_ORPHANED_ROLLBACK_PROOF: ${{ inputs.orphaned_rollback_proof }}
          COMPLETE99_INTERRUPTED_FORWARD_PROOF: ${{ inputs.interrupted_forward_proof }}
          COMPLETE99_ORPHANED_ROLLBACK_OBSERVE_ONLY: ${{ inputs.orphaned_rollback_observe_only }}
          COMPLETE99_INTERRUPTED_FORWARD_OBSERVE_ONLY: ${{ inputs.interrupted_forward_observe_only }}
          COMPLETE99_RECOVERY_ONLY: ${{ inputs.recovery_only }}
        shell: powershell
        run: |
          $hasOrphanedProof = -not [string]::IsNullOrWhiteSpace($env:COMPLETE99_ORPHANED_ROLLBACK_PROOF)
          $hasInterruptedProof = -not [string]::IsNullOrWhiteSpace($env:COMPLETE99_INTERRUPTED_FORWARD_PROOF)
          $orphanedObservation = $env:COMPLETE99_ORPHANED_ROLLBACK_OBSERVE_ONLY.ToLowerInvariant() -eq "true"
          $interruptedObservation = $env:COMPLETE99_INTERRUPTED_FORWARD_OBSERVE_ONLY.ToLowerInvariant() -eq "true"
          $recoveryOnly = $env:COMPLETE99_RECOVERY_ONLY.ToLowerInvariant() -eq "true"

          if ($hasOrphanedProof -and $hasInterruptedProof) {
            Write-Error "Orphaned rollback and interrupted forward proofs are mutually exclusive."
            exit 1
          }
          if ($orphanedObservation -and $interruptedObservation) {
            Write-Error "Orphaned rollback and interrupted forward observation modes are mutually exclusive."
            exit 1
          }
          if ($orphanedObservation -and -not $hasOrphanedProof) {
            Write-Error "Orphaned rollback observation requires orphaned_rollback_proof."
            exit 1
          }
          if ($interruptedObservation -and -not $hasInterruptedProof) {
            Write-Error "Interrupted forward observation requires interrupted_forward_proof."
            exit 1
          }
          if ($hasInterruptedProof -and -not ($interruptedObservation -or $recoveryOnly)) {
            Write-Error "interrupted_forward_proof requires interrupted_forward_observe_only or recovery_only."
            exit 1
          }
          if ($recoveryOnly -and -not $hasInterruptedProof) {
            Write-Error "recovery_only requires interrupted_forward_proof."
            exit 1
          }
          if ($recoveryOnly -and ($orphanedObservation -or $interruptedObservation)) {
            Write-Error "recovery_only cannot be combined with an observation-only mode."
            exit 1
          }

      - name: Require the 1.3.1 rollback and identical-artifact redeploy exercise
        shell: powershell
        run: |
          $releaseDir = Join-Path $env:RUNNER_TEMP "complete99-verified-release-$env:GITHUB_RUN_ID-$env:GITHUB_RUN_ATTEMPT"
          $integrityPath = Join-Path $releaseDir "complete99-platform-integrity.json"
          $releaseMetadata = Get-Content -LiteralPath $integrityPath -Raw | ConvertFrom-Json
          if ($releaseMetadata.version -eq "1.3.1" -and "${{ inputs.rollback_exercise }}" -ne "true") {
            Write-Error "Complete99 Platform 1.3.1 requires rollback_exercise=true before any production request."
            exit 1
          }

      - name: Require the secure sync bootstrap before any live request
        env:
          COMPLETE99_WORDPRESS_SYNC_SECRET: ${{ secrets.COMPLETE99_WORDPRESS_SYNC_SECRET }}
        shell: powershell
        run: |
          if ([string]::IsNullOrEmpty($env:COMPLETE99_WORDPRESS_SYNC_SECRET) -or
              $env:COMPLETE99_WORDPRESS_SYNC_SECRET.Length -lt 32 -or
              $env:COMPLETE99_WORDPRESS_SYNC_SECRET.Length -gt 4096) {
            Write-Error "The production sync secret must be configured with 32 to 4096 characters."
            exit 1
          }

      - name: Run live recovery probe and dry-run acceptance
        id: preflight
        env:
          WP_BASE_URL: ${{ secrets.WP_BASE_URL }}
          WP_DEPLOY_USER: ${{ secrets.WP_DEPLOY_USER }}
          WP_APP_PASSWORD: ${{ secrets.WP_APP_PASSWORD }}
          WP_ALLOWED_DEPLOY_HOSTS: ${{ vars.WP_ALLOWED_DEPLOY_HOSTS }}
          COMPLETE99_ORPHANED_ROLLBACK_PROOF: ${{ inputs.orphaned_rollback_proof }}
          COMPLETE99_INTERRUPTED_FORWARD_PROOF: ${{ inputs.interrupted_forward_proof }}
        shell: powershell
        run: |
          $releaseDir = Join-Path $env:RUNNER_TEMP "complete99-verified-release-$env:GITHUB_RUN_ID-$env:GITHUB_RUN_ATTEMPT"
          $interruptedForwardDist = $releaseDir
          $bootstrapArgs = @()
          $observationArgs = @()
          $interruptedForwardArgs = @()
          $validationArgs = @()
          if ("${{ github.event_name }}" -eq "workflow_dispatch" -and "${{ inputs.bootstrap_code_snippets }}" -eq "true") {
            $bootstrapArgs += "--bootstrap-code-snippets"
          }
          if ("${{ inputs.orphaned_rollback_observe_only }}" -eq "true") {
            $observationArgs += "--observe-orphaned-rollback"
            $validationArgs += "--expect-observation"
          }
          if (-not [string]::IsNullOrWhiteSpace($env:COMPLETE99_INTERRUPTED_FORWARD_PROOF)) {
            $proofRelative = $env:COMPLETE99_INTERRUPTED_FORWARD_PROOF
            $proofSegments = @($proofRelative -split '/')
            $invalidProofSegments = @($proofSegments | Where-Object {
              [string]::IsNullOrWhiteSpace($_) -or $_ -eq '.' -or $_ -eq '..'
            })
            if ($proofRelative.Contains('\') -or
                -not $proofRelative.StartsWith('docs/recovery-proofs/', [System.StringComparison]::Ordinal) -or
                -not $proofRelative.EndsWith('.json', [System.StringComparison]::Ordinal) -or
                $invalidProofSegments.Count -ne 0) {
              Write-Error "The interrupted-forward proof path must be a direct repository-relative recovery-proof JSON path."
              exit 1
            }
            $proofPath = Join-Path $env:GITHUB_WORKSPACE $proofRelative
            try {
              $proofRecord = Get-Content -LiteralPath $proofPath -Raw -ErrorAction Stop | ConvertFrom-Json -ErrorAction Stop
            } catch {
              Write-Error "The interrupted-forward proof could not be read before release selection."
              exit 1
            }
            $proofVersion = [string]$proofRecord.proof.failed_run.version
            if ($proofVersion -notmatch '^[0-9]+\.[0-9]+\.[0-9]+$') {
              Write-Error "The interrupted-forward proof version is invalid."
              exit 1
            }
            $historicalArtifact = Join-Path $env:GITHUB_WORKSPACE "plugin-dist/complete99-platform-$proofVersion.zip"
            $historicalIntegrity = Join-Path $env:GITHUB_WORKSPACE "plugin-dist/complete99-platform-$proofVersion-integrity.json"
            if (-not (Test-Path -LiteralPath $historicalArtifact -PathType Leaf) -or
                -not (Test-Path -LiteralPath $historicalIntegrity -PathType Leaf)) {
              Write-Error "The exact reviewed interrupted-forward release package is unavailable."
              exit 1
            }
            $interruptedForwardDist = Join-Path $env:RUNNER_TEMP "complete99-interrupted-forward-$env:GITHUB_RUN_ID-$env:GITHUB_RUN_ATTEMPT"
            New-Item -ItemType Directory -Path $interruptedForwardDist -ErrorAction Stop | Out-Null
            Copy-Item -LiteralPath $historicalArtifact -Destination (Join-Path $interruptedForwardDist "complete99-platform-$proofVersion.zip") -ErrorAction Stop
            Copy-Item -LiteralPath $historicalIntegrity -Destination (Join-Path $interruptedForwardDist "complete99-platform-integrity.json") -ErrorAction Stop
            $interruptedForwardArgs += @(
              "--interrupted-forward-proof",
              $env:COMPLETE99_INTERRUPTED_FORWARD_PROOF,
              "--dist",
              $interruptedForwardDist
            )
            $validationArgs += @(
              "--interrupted-forward-proof",
              $env:COMPLETE99_INTERRUPTED_FORWARD_PROOF,
              "--expect-interrupted-forward"
            )
          }
          if ("${{ inputs.interrupted_forward_observe_only }}" -eq "true") {
            $interruptedForwardArgs += "--interrupted-forward-observe-only"
            $validationArgs += "--expect-observation"
          }
          if ("${{ inputs.recovery_only }}" -eq "true") {
            $interruptedForwardArgs += "--recovery-only"
          }
          $probeId = "c99-recovery-probe-$env:GITHUB_RUN_ID-$env:GITHUB_RUN_ATTEMPT"
          $recoveryOutput = @(& python scripts/recover-wordpress.py `
            --discover `
            --probe-id $probeId `
            @observationArgs `
            @interruptedForwardArgs `
            @bootstrapArgs)
          $recoveryExitCode = $LASTEXITCODE
          if ($recoveryExitCode -ne 0) { exit $recoveryExitCode }
          $recoverySummary = ($recoveryOutput -join "`n").Trim()
          if ([string]::IsNullOrWhiteSpace($recoverySummary)) {
            Write-Error "The live recovery probe did not return its exact audit summary."
            exit 1
          }
          Write-Output $recoverySummary
          try {
            $recoveryRecord = $recoverySummary | ConvertFrom-Json -ErrorAction Stop
          } catch {
            Write-Error "The live recovery probe returned invalid summary JSON."
            exit 1
          }
          if ($recoveryRecord.result -in @(
                "interrupted_forward_database_mismatch_observed",
                "interrupted_forward_mismatch_diagnostic_observed"
              ) -and
              (($recoveryRecord.PSObject.Properties.Name -notcontains "proof_consumed") -or
               $recoveryRecord.proof_consumed -ne $false)) {
            Write-Error "A mismatch observation must explicitly preserve its v1 proof as unconsumed."
            exit 1
          }
          $env:COMPLETE99_RECOVERY_SUMMARY_JSON = $recoverySummary
          & python scripts/validate-recovery-audit.py `
            --expected-probe-id $probeId `
            --dist $interruptedForwardDist `
            @validationArgs
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          if (-not [string]::IsNullOrWhiteSpace($env:COMPLETE99_INTERRUPTED_FORWARD_PROOF) -and
              $recoveryRecord.result -in @("recovered", "already-recovered")) {
            "platform_recovered=true" >> $env:GITHUB_OUTPUT
          }
          if ("${{ inputs.orphaned_rollback_observe_only }}" -eq "true") {
            exit 0
          }
          if ("${{ inputs.interrupted_forward_observe_only }}" -eq "true") {
            exit 0
          }

          $dryId = "c99-dry-$env:GITHUB_RUN_ID-$env:GITHUB_RUN_ATTEMPT"
          & python scripts/deploy-wordpress.py `
            --dist $releaseDir `
            --dry-run `
            --deployment-id $dryId `
            @bootstrapArgs
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

          $auditVerification = @'
          import json
          import pathlib
          import sys
          def require(condition, message):
              if not condition:
                  raise SystemExit(message)
          record = json.loads((pathlib.Path("deploy-audit") / f"{sys.argv[1]}.json").read_text())
          cleanup = record.get("cleanup", {})
          require(record.get("deployment_id") == sys.argv[1], "Dry-run audit deployment ID changed")
          require(record.get("result") == "dry-run-passed", "Dry-run audit did not pass")
          require(cleanup.get("snippet_active") is False, "Dry-run bridge remained active")
          require(cleanup.get("row_absence_verified") is True, "Dry-run bridge row remained")
          require(cleanup.get("route_404") is True, "Dry-run bridge route remained")
          '@
          $auditVerification | python - $dryId
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

      - name: Deploy the exact CI artifact with independent verification
        id: production_deploy
        if: inputs.orphaned_rollback_observe_only != true && inputs.interrupted_forward_observe_only != true && inputs.recovery_only != true
        env:
          WP_BASE_URL: ${{ secrets.WP_BASE_URL }}
          WP_DEPLOY_USER: ${{ secrets.WP_DEPLOY_USER }}
          WP_APP_PASSWORD: ${{ secrets.WP_APP_PASSWORD }}
          COMPLETE99_WORDPRESS_SYNC_SECRET: ${{ secrets.COMPLETE99_WORDPRESS_SYNC_SECRET }}
          WP_ALLOWED_DEPLOY_HOSTS: ${{ vars.WP_ALLOWED_DEPLOY_HOSTS }}
        shell: powershell
        run: |
          $releaseDir = Join-Path $env:RUNNER_TEMP "complete99-verified-release-$env:GITHUB_RUN_ID-$env:GITHUB_RUN_ATTEMPT"
          $mutationMarker = Join-Path $env:RUNNER_TEMP "complete99-live-mutation-$env:GITHUB_RUN_ID-$env:GITHUB_RUN_ATTEMPT.marker"
          $deployArgs = @()
          if ("${{ github.event_name }}" -eq "workflow_dispatch" -and "${{ inputs.rollback_exercise }}" -eq "true") {
            $deployArgs += "--rollback-exercise"
          }
          & python scripts/deploy-wordpress.py `
            --dist $releaseDir `
            --deployment-id "c99-prod-$env:GITHUB_RUN_ID-$env:GITHUB_RUN_ATTEMPT" `
            --mutation-marker $mutationMarker `
            @deployArgs
          exit $LASTEXITCODE

      - name: Detect whether the production mutation edge was crossed
        id: mutation_state
        if: failure() && steps.production_deploy.outcome == 'failure' && inputs.orphaned_rollback_observe_only != true && inputs.interrupted_forward_observe_only != true && inputs.recovery_only != true
        shell: powershell
        run: |
          $mutationMarker = Join-Path $env:RUNNER_TEMP "complete99-live-mutation-$env:GITHUB_RUN_ID-$env:GITHUB_RUN_ATTEMPT.marker"
          if (Test-Path -LiteralPath $mutationMarker -PathType Leaf) {
            "started=true" >> $env:GITHUB_OUTPUT
          } else {
            "started=false" >> $env:GITHUB_OUTPUT
          }

      - name: Recover any interrupted mutation with a recreated temporary bridge
        id: recovery
        if: failure() && steps.production_deploy.outcome == 'failure' && steps.mutation_state.outputs.started == 'true' && inputs.orphaned_rollback_observe_only != true && inputs.interrupted_forward_observe_only != true && inputs.recovery_only != true
        env:
          WP_BASE_URL: ${{ secrets.WP_BASE_URL }}
          WP_DEPLOY_USER: ${{ secrets.WP_DEPLOY_USER }}
          WP_APP_PASSWORD: ${{ secrets.WP_APP_PASSWORD }}
          WP_ALLOWED_DEPLOY_HOSTS: ${{ vars.WP_ALLOWED_DEPLOY_HOSTS }}
        shell: powershell
        run: |
          $bootstrapArgs = @()
          if ("${{ github.event_name }}" -eq "workflow_dispatch" -and "${{ inputs.bootstrap_code_snippets }}" -eq "true") {
            $bootstrapArgs += "--bootstrap-code-snippets"
          }
          & python scripts/recover-wordpress.py `
            --deployment-id "c99-prod-$env:GITHUB_RUN_ID-$env:GITHUB_RUN_ATTEMPT" `
            @bootstrapArgs
          exit $LASTEXITCODE

      - name: Remove the runner-local mutation marker
        if: always()
        shell: powershell
        run: |
          $mutationMarker = Join-Path $env:RUNNER_TEMP "complete99-live-mutation-$env:GITHUB_RUN_ID-$env:GITHUB_RUN_ATTEMPT.marker"
          if (Test-Path -LiteralPath $mutationMarker -PathType Leaf) {
            Remove-Item -LiteralPath $mutationMarker -Force
          }

      - name: Install pinned WooCommerce and materialize the live catalog
        id: commerce
        if: inputs.orphaned_rollback_observe_only != true && inputs.interrupted_forward_observe_only != true
        continue-on-error: true
        env:
          WP_BASE_URL: ${{ secrets.WP_BASE_URL }}
          WP_DEPLOY_USER: ${{ secrets.WP_DEPLOY_USER }}
          WP_APP_PASSWORD: ${{ secrets.WP_APP_PASSWORD }}
          WP_ALLOWED_DEPLOY_HOSTS: ${{ vars.WP_ALLOWED_DEPLOY_HOSTS }}
        shell: powershell
        run: |
          & python scripts/materialize-woocommerce.py `
            --deployment-id "c99-commerce-$env:GITHUB_RUN_ID-$env:GITHUB_RUN_ATTEMPT"
          exit $LASTEXITCODE

      - name: Recover an interrupted WooCommerce bridge
        id: commerce_recovery
        if: steps.commerce.outcome == 'failure'
        env:
          WP_BASE_URL: ${{ secrets.WP_BASE_URL }}
          WP_DEPLOY_USER: ${{ secrets.WP_DEPLOY_USER }}
          WP_APP_PASSWORD: ${{ secrets.WP_APP_PASSWORD }}
          WP_ALLOWED_DEPLOY_HOSTS: ${{ vars.WP_ALLOWED_DEPLOY_HOSTS }}
        shell: powershell
        run: |
          & python scripts/materialize-woocommerce.py `
            --deployment-id "c99-commerce-$env:GITHUB_RUN_ID-$env:GITHUB_RUN_ATTEMPT" `
            --recover-bridge
          exit $LASTEXITCODE

      - name: Validate exact deployment-stage audit outcomes
        if: always()
        env:
          COMPLETE99_PREFLIGHT_OUTCOME: ${{ steps.preflight.outcome }}
          COMPLETE99_PRODUCTION_OUTCOME: ${{ steps.production_deploy.outcome }}
          COMPLETE99_MUTATION_OUTCOME: ${{ steps.mutation_state.outcome }}
          COMPLETE99_MUTATION_STARTED: ${{ steps.mutation_state.outputs.started }}
          COMPLETE99_RECOVERY_OUTCOME: ${{ steps.recovery.outcome }}
          COMPLETE99_COMMERCE_OUTCOME: ${{ steps.commerce.outcome }}
          COMPLETE99_COMMERCE_RECOVERY_OUTCOME: ${{ steps.commerce_recovery.outcome }}
          COMPLETE99_OBSERVATION_ONLY: ${{ inputs.orphaned_rollback_observe_only || inputs.interrupted_forward_observe_only }}
          COMPLETE99_RECOVERY_ONLY: ${{ inputs.recovery_only }}
          COMPLETE99_PLATFORM_RECOVERED: ${{ steps.preflight.outputs.platform_recovered }}
        shell: powershell
        run: |
          & python scripts/validate-recovery-audit.py `
            --stage-outcomes `
            --dry-run-id "c99-dry-$env:GITHUB_RUN_ID-$env:GITHUB_RUN_ATTEMPT" `
            --production-id "c99-prod-$env:GITHUB_RUN_ID-$env:GITHUB_RUN_ATTEMPT" `
            --commerce-id "c99-commerce-$env:GITHUB_RUN_ID-$env:GITHUB_RUN_ATTEMPT"
          exit $LASTEXITCODE

      - name: Fail closed after an audited commerce failure
        if: always() && steps.commerce.outcome == 'failure'
        shell: powershell
        run: |
          Write-Error "WooCommerce or catalog verification failed. The recovery and audit steps have completed."
          exit 1

      - name: Upload non-secret deployment audit
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: complete99-wordpress-deploy-audit
          path: |
            deploy-audit/*.json
            recovery-audit/*.json
            commerce-audit/*.json
          if-no-files-found: error
          retention-days: 30

  verify-campaign-worker:
    name: Gate the final deployed Campaign worker heartbeat
    if: ${{ always() && inputs.orphaned_rollback_observe_only != true && inputs.interrupted_forward_observe_only != true && inputs.recovery_only != true }}
    needs: deploy
    runs-on: [self-hosted, Windows, X64, complete99-monitor]
    environment: campaign-monitor
    timeout-minutes: 10
    steps:
      - name: Require a successful normal deployment outcome
        env:
          COMPLETE99_DEPLOY_OUTCOME: ${{ needs.deploy.result }}
          COMPLETE99_ROLLBACK_EXERCISE: ${{ inputs.rollback_exercise }}
        shell: powershell
        run: |
          if ($env:COMPLETE99_DEPLOY_OUTCOME -ne "success") {
            Write-Error "The deployment job did not complete successfully."
            exit 1
          }
          if ($env:COMPLETE99_ROLLBACK_EXERCISE.ToLowerInvariant() -eq "true") {
            Write-Output "Campaign worker verification is deferred until the final identical-artifact redeploy is complete."
          }

      - name: Check out the exact deployed source for the worker gate
        if: inputs.rollback_exercise != true
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          fetch-depth: 1
          persist-credentials: false
          sparse-checkout: |
            scripts/monitor-campaign-worker.py
          sparse-checkout-cone-mode: false

      - name: Require a fresh Campaign worker heartbeat from the final release
        if: inputs.rollback_exercise != true
        env:
          WP_BASE_URL: ${{ secrets.WP_BASE_URL }}
          WP_CAMPAIGN_WORKER_USER: ${{ secrets.WP_CAMPAIGN_WORKER_USER }}
          WP_CAMPAIGN_WORKER_APP_PASSWORD: ${{ secrets.WP_CAMPAIGN_WORKER_APP_PASSWORD }}
          WP_ALLOWED_MONITOR_HOSTS: ${{ secrets.WP_ALLOWED_MONITOR_HOSTS }}
        shell: powershell
        run: |
          & python --version
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          & python scripts/monitor-campaign-worker.py
          exit $LASTEXITCODE
